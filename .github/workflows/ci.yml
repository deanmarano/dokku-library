name: CI

on:
  push:
    branches: [main, trunk]
  pull_request:
    branches: [main, trunk]
    types: [opened, synchronize, reopened, labeled]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install shellcheck
        run: sudo apt-get install -y shellcheck
      - name: Lint bash scripts
        run: shellcheck -x commands config install functions subcommands/*

  unit-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Install dependencies
        run: npm ci
      - name: Run unit tests
        run: npm run test:unit

  # --- E2E jobs: enabling one-by-one ---
  # Known issues remaining:
  #   - dokku-auth repo doesn't exist yet (all e2e-app jobs)

  e2e-checkout:
    needs: [lint, unit-tests]
    runs-on: ubuntu-22.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Install dependencies
        run: npm ci && npx playwright install --with-deps
      - name: Install Dokku
        run: |
          wget -NP . https://packagecloud.io/dokku/dokku/gpgkey
          sudo apt-key add gpgkey
          echo "deb https://packagecloud.io/dokku/dokku/ubuntu/ jammy main" | sudo tee /etc/apt/sources.list.d/dokku.list
          sudo apt-get update
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y dokku
          sudo dokku plugin:install-dependencies --core
      - name: Install dokku-postgres
        run: sudo dokku plugin:install https://github.com/dokku/dokku-postgres.git postgres
      - name: Install library plugin
        run: |
          # Create plugin directory and copy essential files
          sudo mkdir -p /var/lib/dokku/plugins/available/library/subcommands
          sudo mkdir -p /var/lib/dokku/plugins/available/library/library
          sudo cp commands config functions install plugin.toml /var/lib/dokku/plugins/available/library/
          sudo cp subcommands/* /var/lib/dokku/plugins/available/library/subcommands/
          sudo cp -r library/* /var/lib/dokku/plugins/available/library/library/
          # Make scripts executable
          sudo chmod +x /var/lib/dokku/plugins/available/library/commands
          sudo chmod +x /var/lib/dokku/plugins/available/library/subcommands/*
          # plugin:enable exits 10 during bash-completion priming (DOKKU_NOT_IMPLEMENTED_EXIT) — expected
          sudo dokku plugin:enable library || true
          # Run the plugin install hook to create data directories
          sudo bash /var/lib/dokku/plugins/enabled/library/install
          # Verify the plugin is registered
          sudo dokku library:list
      - name: Deploy test app via bash
        run: |
          # dokku's plugn/go-basher dispatch requires a bash parent process for
          # complex commands like git:from-image. Run checkout from bash directly.
          echo "127.0.0.1 test-uptime-kuma.test.local" | sudo tee -a /etc/hosts
          sudo dokku library:checkout uptime-kuma \
            --name=test-uptime-kuma \
            --domain=test-uptime-kuma.test.local \
            --no-ssl --no-auth --non-interactive
      - name: Verify deployment state
        run: |
          echo "=== Dokku version ==="
          dokku version || true
          echo "=== App exists? ==="
          sudo dokku apps:exists test-uptime-kuma && echo "YES" || echo "NO"
          echo "=== ps:report ==="
          sudo dokku ps:report test-uptime-kuma 2>&1 || true
          echo "=== Docker containers ==="
          sudo docker ps -a 2>&1 || true
          echo "=== Library state directory ==="
          ls -laR /var/lib/dokku/services/library/apps/test-uptime-kuma/ 2>&1 || echo "STATE DIR MISSING"
          echo "=== Ports config ==="
          sudo dokku ports:report test-uptime-kuma 2>&1 || true
          echo "=== Proxy config ==="
          sudo dokku proxy:report test-uptime-kuma 2>&1 || true
          echo "=== Nginx config ==="
          sudo cat /home/dokku/test-uptime-kuma/nginx.conf 2>&1 || echo "No nginx.conf"
          echo "=== DEPLOYED flag ==="
          ls -la /home/dokku/test-uptime-kuma/DEPLOYED 2>&1 || echo "DEPLOYED file missing"
      - name: Run checkout e2e tests
        run: npx playwright test tests/e2e/checkout.spec.ts --project=integration
      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: e2e-checkout-results
          path: playwright-report/
      - name: Collect logs on failure
        if: failure()
        run: |
          echo "=== Plugin paths ==="
          ls -la /var/lib/dokku/plugins/available/library/ || true
          ls -la /var/lib/dokku/plugins/enabled/library/ || true
          echo "=== Try running library:list ==="
          sudo dokku library:list 2>&1 || true
          echo "=== Try running library:list --installed ==="
          sudo dokku library:list --installed 2>&1 || true
          echo "=== Dokku apps ==="
          sudo dokku apps:list 2>/dev/null || true
          echo "=== ps:report ==="
          sudo dokku ps:report 2>/dev/null || true
          echo "=== Docker containers ==="
          sudo docker ps -a 2>/dev/null || true
          echo "=== Docker logs ==="
          for container in $(sudo docker ps -aq 2>/dev/null); do
            echo "--- Container $container ---"
            sudo docker logs --tail 50 "$container" 2>/dev/null || true
          done
          echo "=== Library state ==="
          ls -laR /var/lib/dokku/services/library/apps/ 2>&1 || echo "No state dir"
          echo "=== DEPLOYED flag ==="
          ls -la /home/dokku/test-uptime-kuma/DEPLOYED 2>&1 || echo "DEPLOYED file missing"

  e2e-lifecycle:
    needs: [lint, unit-tests]
    runs-on: ubuntu-22.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Install dependencies
        run: npm ci && npx playwright install --with-deps
      - name: Install Dokku
        run: |
          wget -NP . https://packagecloud.io/dokku/dokku/gpgkey
          sudo apt-key add gpgkey
          echo "deb https://packagecloud.io/dokku/dokku/ubuntu/ jammy main" | sudo tee /etc/apt/sources.list.d/dokku.list
          sudo apt-get update
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y dokku
          sudo dokku plugin:install-dependencies --core
      - name: Install dokku-postgres
        run: sudo dokku plugin:install https://github.com/dokku/dokku-postgres.git postgres
      - name: Install library plugin
        run: |
          sudo mkdir -p /var/lib/dokku/plugins/available/library/subcommands
          sudo mkdir -p /var/lib/dokku/plugins/available/library/library
          sudo cp commands config functions install plugin.toml /var/lib/dokku/plugins/available/library/
          sudo cp subcommands/* /var/lib/dokku/plugins/available/library/subcommands/
          sudo cp -r library/* /var/lib/dokku/plugins/available/library/library/
          sudo chmod +x /var/lib/dokku/plugins/available/library/commands
          sudo chmod +x /var/lib/dokku/plugins/available/library/subcommands/*
          sudo dokku plugin:enable library || true
          sudo bash /var/lib/dokku/plugins/enabled/library/install
          sudo dokku library:list
      - name: Deploy test app via bash
        run: |
          echo "127.0.0.1 test-lifecycle-app.test.local" | sudo tee -a /etc/hosts
          sudo dokku library:checkout uptime-kuma \
            --name=test-lifecycle-app \
            --domain=test-lifecycle-app.test.local \
            --no-ssl --no-auth --non-interactive
      - name: Verify deployment state
        run: |
          echo "=== App exists? ==="
          sudo dokku apps:exists test-lifecycle-app && echo "YES" || echo "NO"
          echo "=== ps:report ==="
          sudo dokku ps:report test-lifecycle-app 2>&1 || true
          echo "=== Library state ==="
          ls -laR /var/lib/dokku/services/library/apps/test-lifecycle-app/ 2>&1 || echo "STATE DIR MISSING"
      - name: Run lifecycle e2e tests
        run: npx playwright test tests/e2e/lifecycle.spec.ts --project=integration
      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: e2e-lifecycle-results
          path: playwright-report/
      - name: Collect logs on failure
        if: failure()
        run: |
          echo "=== Dokku apps ==="
          sudo dokku apps:list 2>/dev/null || true
          echo "=== ps:report ==="
          sudo dokku ps:report 2>/dev/null || true
          echo "=== Docker containers ==="
          sudo docker ps -a 2>/dev/null || true
          echo "=== Docker logs ==="
          for container in $(sudo docker ps -aq 2>/dev/null); do
            echo "--- Container $container ---"
            sudo docker logs --tail 50 "$container" 2>/dev/null || true
          done
          echo "=== Library state ==="
          ls -laR /var/lib/dokku/services/library/apps/ 2>&1 || echo "No state dir"

  e2e-doctor:
    needs: [lint, unit-tests]
    runs-on: ubuntu-22.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Install dependencies
        run: npm ci && npx playwright install --with-deps
      - name: Install Dokku
        run: |
          wget -NP . https://packagecloud.io/dokku/dokku/gpgkey
          sudo apt-key add gpgkey
          echo "deb https://packagecloud.io/dokku/dokku/ubuntu/ jammy main" | sudo tee /etc/apt/sources.list.d/dokku.list
          sudo apt-get update
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y dokku
          sudo dokku plugin:install-dependencies --core
      - name: Install dokku-postgres
        run: sudo dokku plugin:install https://github.com/dokku/dokku-postgres.git postgres
      - name: Install library plugin
        run: |
          sudo mkdir -p /var/lib/dokku/plugins/available/library/subcommands
          sudo mkdir -p /var/lib/dokku/plugins/available/library/library
          sudo cp commands config functions install plugin.toml /var/lib/dokku/plugins/available/library/
          sudo cp subcommands/* /var/lib/dokku/plugins/available/library/subcommands/
          sudo cp -r library/* /var/lib/dokku/plugins/available/library/library/
          sudo chmod +x /var/lib/dokku/plugins/available/library/commands
          sudo chmod +x /var/lib/dokku/plugins/available/library/subcommands/*
          sudo dokku plugin:enable library || true
          sudo bash /var/lib/dokku/plugins/enabled/library/install
          sudo dokku library:list
      - name: Deploy test app via bash
        run: |
          echo "127.0.0.1 test-doctor-app.test.local" | sudo tee -a /etc/hosts
          sudo dokku library:checkout uptime-kuma \
            --name=test-doctor-app \
            --domain=test-doctor-app.test.local \
            --no-ssl --no-auth --non-interactive
      - name: Verify deployment state
        run: |
          echo "=== App exists? ==="
          sudo dokku apps:exists test-doctor-app && echo "YES" || echo "NO"
          echo "=== ps:report ==="
          sudo dokku ps:report test-doctor-app 2>&1 || true
          echo "=== Library state ==="
          ls -laR /var/lib/dokku/services/library/apps/test-doctor-app/ 2>&1 || echo "STATE DIR MISSING"
          echo "=== git:report ==="
          sudo dokku git:report test-doctor-app 2>&1 || true
          echo "=== domains:report ==="
          sudo dokku domains:report test-doctor-app 2>&1 || true
          echo "=== ports:report ==="
          sudo dokku ports:report test-doctor-app 2>&1 || true
      - name: Run doctor e2e tests
        run: npx playwright test tests/e2e/doctor.spec.ts --project=integration
      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: e2e-doctor-results
          path: playwright-report/
      - name: Collect logs on failure
        if: failure()
        run: |
          echo "=== Dokku apps ==="
          sudo dokku apps:list 2>/dev/null || true
          echo "=== ps:report ==="
          sudo dokku ps:report 2>/dev/null || true
          echo "=== Docker containers ==="
          sudo docker ps -a 2>/dev/null || true
          echo "=== Docker logs ==="
          for container in $(sudo docker ps -aq 2>/dev/null); do
            echo "--- Container $container ---"
            sudo docker logs --tail 50 "$container" 2>/dev/null || true
          done
          echo "=== Library state ==="
          ls -laR /var/lib/dokku/services/library/apps/ 2>&1 || echo "No state dir"
          echo "=== doctor output ==="
          sudo dokku library:doctor test-doctor-app 2>&1 || true

  # e2e-ghost:
  #   needs: [lint, unit-tests]
  #   if: github.event_name == 'push'
  #   runs-on: ubuntu-22.04
  #   timeout-minutes: 30
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: "20"
  #     - name: Install dependencies
  #       run: npm ci && npx playwright install --with-deps
  #     - name: Install Dokku
  #       run: |
  #         wget -NP . https://packagecloud.io/dokku/dokku/gpgkey
  #         sudo apt-key add gpgkey
  #         echo "deb https://packagecloud.io/dokku/dokku/ubuntu/ jammy main" | sudo tee /etc/apt/sources.list.d/dokku.list
  #         sudo apt-get update
  #         sudo DEBIAN_FRONTEND=noninteractive apt-get install -y dokku
  #         sudo dokku plugin:install-dependencies --core
  #     - name: Install dokku-postgres
  #       run: sudo dokku plugin:install https://github.com/dokku/dokku-postgres.git postgres
  #     - name: Install library plugin
  #       run: |
  #         sudo ln -s ${{ github.workspace }} /var/lib/dokku/plugins/available/library
  #         sudo dokku plugin:enable library
  #     - name: Run ghost e2e tests
  #       run: npx playwright test ghost
  #     - name: Upload test results
  #       uses: actions/upload-artifact@v4
  #       if: always()
  #       with:
  #         name: e2e-ghost-results
  #         path: playwright-report/
  #     - name: Collect logs on failure
  #       if: failure()
  #       run: |
  #         sudo dokku apps:list 2>/dev/null || true
  #         sudo dokku ps:report 2>/dev/null || true
  #         sudo docker ps -a 2>/dev/null || true

  # e2e-doctor:
  #   needs: [lint, unit-tests]
  #   runs-on: ubuntu-22.04
  #   timeout-minutes: 30
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: "20"
  #     - name: Install dependencies
  #       run: npm ci && npx playwright install --with-deps
  #     - name: Install Dokku
  #       run: |
  #         wget -NP . https://packagecloud.io/dokku/dokku/gpgkey
  #         sudo apt-key add gpgkey
  #         echo "deb https://packagecloud.io/dokku/dokku/ubuntu/ jammy main" | sudo tee /etc/apt/sources.list.d/dokku.list
  #         sudo apt-get update
  #         sudo DEBIAN_FRONTEND=noninteractive apt-get install -y dokku
  #         sudo dokku plugin:install-dependencies --core
  #     - name: Install dokku-postgres
  #       run: sudo dokku plugin:install https://github.com/dokku/dokku-postgres.git postgres
  #     - name: Install library plugin
  #       run: |
  #         sudo ln -s ${{ github.workspace }} /var/lib/dokku/plugins/available/library
  #         sudo dokku plugin:enable library
  #     - name: Run doctor e2e tests
  #       run: npx playwright test doctor
  #     - name: Upload test results
  #       uses: actions/upload-artifact@v4
  #       if: always()
  #       with:
  #         name: e2e-doctor-results
  #         path: playwright-report/
  #     - name: Collect logs on failure
  #       if: failure()
  #       run: |
  #         sudo dokku apps:list 2>/dev/null || true
  #         sudo dokku ps:report 2>/dev/null || true
  #         sudo docker ps -a 2>/dev/null || true

  # e2e-app:
  #   needs: [lint, unit-tests, changes]
  #   if: ${{ needs.changes.outputs.app-matrix != '[]' }}
  #   strategy:
  #     fail-fast: false
  #     matrix:
  #       app: ${{ fromJson(needs.changes.outputs.app-matrix) }}
  #   runs-on: ubuntu-22.04
  #   timeout-minutes: 30
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: "20"
  #     - name: Install dependencies
  #       run: npm ci && npx playwright install --with-deps
  #     - name: Install Dokku
  #       run: |
  #         wget -NP . https://packagecloud.io/dokku/dokku/gpgkey
  #         sudo apt-key add gpgkey
  #         echo "deb https://packagecloud.io/dokku/dokku/ubuntu/ jammy main" | sudo tee /etc/apt/sources.list.d/dokku.list
  #         sudo apt-get update
  #         sudo DEBIAN_FRONTEND=noninteractive apt-get install -y dokku
  #         sudo dokku plugin:install-dependencies --core
  #     - name: Install dokku-postgres
  #       run: sudo dokku plugin:install https://github.com/dokku/dokku-postgres.git postgres
  #     - name: Install dokku-auth
  #       run: sudo dokku plugin:install https://github.com/deanmarano/dokku-auth.git auth
  #     - name: Install library plugin
  #       run: |
  #         sudo ln -s ${{ github.workspace }} /var/lib/dokku/plugins/available/library
  #         sudo dokku plugin:enable library
  #     - name: Run ${{ matrix.app }} e2e tests
  #       run: npx playwright test tests/e2e/apps/${{ matrix.app }}.spec.ts
  #     - name: Upload test results
  #       uses: actions/upload-artifact@v4
  #       if: always()
  #       with:
  #         name: e2e-app-${{ matrix.app }}-results
  #         path: |
  #           playwright-report/
  #           test-results/
  #     - name: Collect logs on failure
  #       if: failure()
  #       run: |
  #         echo "=== Dokku apps ==="
  #         sudo dokku apps:list 2>/dev/null || true
  #         echo "=== Process report ==="
  #         sudo dokku ps:report 2>/dev/null || true
  #         echo "=== Docker containers ==="
  #         sudo docker ps -a 2>/dev/null || true
  #         echo "=== Auth services ==="
  #         sudo dokku auth:list 2>/dev/null || true
  #         sudo dokku auth:frontend:list 2>/dev/null || true
  #         echo "=== Docker logs ==="
  #         for container in $(sudo docker ps -aq 2>/dev/null); do
  #           echo "--- Container $container ---"
  #           sudo docker logs --tail 50 "$container" 2>/dev/null || true
  #         done
